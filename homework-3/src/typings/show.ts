export interface IShow {
  id: string;
  title: string;
  averageRating: number;
  description?: string;
  imageUrl?: string;
}

export interface IShowCard  {
  id: string;
  title: string;
  averageRating: number;
  imageUrl: string;
}